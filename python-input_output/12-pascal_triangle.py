#!/usr/bin/python3

def pascal_triangle(n):
    list = []
    if n == 0:
        return list
    for i in range(1:n):
        inner_list = []
        for j in range(i:n):
        inner_list.extend(
            
        )
        list.extend(inner_list)
    